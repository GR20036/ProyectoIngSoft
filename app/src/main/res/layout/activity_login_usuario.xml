<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Login_Usuario">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="20dp"
        android:background="#FFFFFF">

        <TextView
            android:id="@+id/loginUsuarioLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Iniciar Sesión como usuario"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#000000"
            android:layout_marginBottom="50sp"/>

        <ImageView
            android:layout_width="180sp"
            android:layout_height="180sp"
            android:src="@drawable/logo_agendify"
            android:layout_marginBottom="30sp"/>

        <EditText
            android:id="@+id/emailUsuarioEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Correo Electrónico"
            android:inputType="textEmailAddress"
            android:layout_marginBottom="30sp"/>

        <EditText
            android:id="@+id/passwordUsuarioEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Contraseña"
            android:inputType="textPassword"
            android:layout_marginBottom="50sp"/>

        <Button
            android:id="@+id/loginUsuarioButton"
            android:layout_width="150sp"
            android:layout_height="wrap_content"
            android:text="Iniciar sesión"
            android:textColor="#FFFFFF"
            android:background="@drawable/boton_personalizado"/>

    </LinearLayout>

    <!-- ImageButton para desplegar el menú -->
    <ImageButton
        android:id="@+id/menuButton2"
        android:layout_width="40sp"
        android:layout_height="40sp"
        android:background="#00FFFFFF"
        android:contentDescription="Menú desplegable"
        android:src="@drawable/ic_menu"
        android:layout_marginTop="20sp"
        android:layout_marginEnd="20sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>